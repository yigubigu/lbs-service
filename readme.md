# Mybatis generate mybatis from database
mvn mybatis-generator:generate

# Debug
mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=y,address=5005"
